# HTML Preview

A tool our administrators at work can use to preview how the HTML-entities will look on the actual application. The reason is to catch errors and allow the administrators to be sure of that the HTML won't break.
